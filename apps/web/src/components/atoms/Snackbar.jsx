import React from 'react'

const Snackbar = ({variant, message}) => {
    const variants = {
        success: "border border-green-500 text-green-500 bg-green-100"
    }
  return (
    <div className='absolute bottom-5 right-5 rounded'>
      
    </div>
  )
}

export default Snackbar
